spring:
  security:
    debug: true

  datasource:
    url: jdbc:mysql://mysql:3306/camstudy
    username: camuser
    password: campass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  address: 0.0.0.0
  port: 8080

logging:
  level:
    org.springframework.security: TRACE
